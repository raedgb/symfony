<form method="post" action="{{ path('app_colis_delete', {'idColis': coli.idColis}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coli.idColis) }}">
    <button class="btn">Delete</button>
</form>
