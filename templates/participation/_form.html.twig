{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary btn-sm" formnovalidate>{{ button_label|default('Save') }}</button>
    <a class="btn btn-primary btn-sm" href="{{ path('app_participation_index') }}">back to list</a>
{{ form_end(form) }}
<style>
    
    .btn-primary {
      background-color: pink;
      border-color: pink;
    }
  
    
  </style>